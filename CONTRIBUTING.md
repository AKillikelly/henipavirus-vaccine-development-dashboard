# Contributing

1. Edit curated records in `config/pipeline.yml`.
2. Add public sources for any proposed row or stage change.
3. Run `make update-offline` and `make validate` before opening a pull request.
4. Use review flags as prompts for manual curation, not as automatic evidence of stage changes.
